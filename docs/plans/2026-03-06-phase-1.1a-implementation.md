# Phase 1.1a - Settings & Infrastructure Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Implement user-scoped API Keys with authentication and comprehensive audit logging system.

**Architecture:**
- **Backend**: Python/SQLAlchemy models with FastAPI endpoints, middleware for request interception
- **Frontend**: React components with shadcn/ui, tabs for Settings page navigation
- **Database**: PostgreSQL with JSONB for scopes/details, indexed queries for performance
- **Security**: SHA256 key hashing, scope-based permissions, immutable audit logs

**Tech Stack:**
- Backend: FastAPI, SQLAlchemy, Pydantic, pytest
- Frontend: React 18, TypeScript, React Query, Zustand, shadcn/ui
- Database: PostgreSQL 15+, Alembic migrations

---

## Task 1: Create API Key Database Model and Migration

**Files:**
- Create: `backend/models/api_key.py`
- Create: `backend/alembic/versions/xxx_create_api_keys_table.py`
- Modify: `backend/models/__init__.py`

**Step 1: Write the API Key model**

Create `backend/models/api_key.py`:

```python
from sqlalchemy import Column, String, Boolean, DateTime, ForeignKey, JSON
from sqlalchemy.orm import relationship
from datetime import datetime, timezone
import uuid
from backend.models.base import Base

class APIKey(Base):
    """User-scoped API key for authentication"""
    __tablename__ = "api_keys"

    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    user_id = Column(String(36), ForeignKey("users.id", ondelete="CASCADE"), nullable=False, index=True)
    name = Column(String(255), nullable=False)
    key_hash = Column(String(255), nullable=False, unique=True, index=True)
    key_prefix = Column(String(10), nullable=False)
    scopes = Column(JSON, nullable=False, default=list)
    expires_at = Column(DateTime(timezone=True), nullable=True)
    last_used_at = Column(DateTime(timezone=True), nullable=True)
    is_active = Column(Boolean, default=True, nullable=False, index=True)
    created_at = Column(DateTime(timezone=True), default=lambda: datetime.now(timezone.utc), nullable=False)

    user = relationship("User", back_populates="api_keys")

    def __repr__(self):
        return f"<APIKey(id={self.id}, name={self.name}, prefix={self.key_prefix})>"
```

**Step 2: Update User model to add relationship**

Modify `backend/models/user.py`, add to User class:

```python
# Add near other relationships
api_keys = relationship("APIKey", back_populates="user", cascade="all, delete-orphan")
```

**Step 3: Update models __init__.py**

Modify `backend/models/__init__.py`, add import:

```python
from backend.models.api_key import APIKey  # Add this line
```

**Step 4: Create Alembic migration**

Create `backend/alembic/versions/001_create_api_keys_table.py`:

```python
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = '001_create_api_keys'
down_revision = None  # Set to your latest migration
branch_labels = None
depends_on = None

def upgrade():
    op.create_table(
        'api_keys',
        sa.Column('id', sa.String(36), primary_key=True),
        sa.Column('user_id', sa.String(36), sa.ForeignKey('users.id', ondelete='CASCADE'), nullable=False),
        sa.Column('name', sa.String(255), nullable=False),
        sa.Column('key_hash', sa.String(255), nullable=False, unique=True),
        sa.Column('key_prefix', sa.String(10), nullable=False),
        sa.Column('scopes', postgresql.JSON(), nullable=False, server_default='[]'),
        sa.Column('expires_at', sa.DateTime(timezone=True), nullable=True),
        sa.Column('last_used_at', sa.DateTime(timezone=True), nullable=True),
        sa.Column('is_active', sa.Boolean(), nullable=False, server_default='true'),
        sa.Column('created_at', sa.DateTime(timezone=True), nullable=False, server_default=sa.text('now()')),
        sa.Index('ix_api_keys_user_id', 'user_id'),
        sa.Index('ix_api_keys_key_hash', 'key_hash'),
        sa.Index('ix_api_keys_is_active', 'is_active'),
    )

def downgrade():
    op.drop_table('api_keys')
```

**Step 5: Run migration**

Run: `cd backend && alembic upgrade head`
Expected: Output showing migration applied successfully

**Step 6: Commit**

```bash
git add backend/models/api_key.py backend/models/user.py backend/models/__init__.py backend/alembic/versions/001_create_api_keys_table.py
git commit -m "feat(api-key): add API key database model and migration"
```

---

## Task 2: Create API Key Service Layer

**Files:**
- Create: `backend/services/api_key_service.py`
- Create: `backend/schemas/api_key.py`

**Step 1: Write API Key Pydantic schemas**

Create `backend/schemas/api_key.py`:

```python
from pydantic import BaseModel, Field
from datetime import datetime
from typing import List, Optional

class APIScope(str):
    """API key permission scopes"""
    RESOURCES_READ = "resources:read"
    RESOURCES_WRITE = "resources:write"
    SKILLS_READ = "skills:read"
    SKILLS_CALL = "skills:call"
    SKILLS_WRITE = "skills:write"
    ACL_READ = "acl:read"
    ACL_WRITE = "acl:write"
    TOKENS_READ = "tokens:read"
    TOKENS_WRITE = "tokens:write"

    @classmethod
    def all_scopes(cls):
        return [
            cls.RESOURCES_READ, cls.RESOURCES_WRITE,
            cls.SKILLS_READ, cls.SKILLS_CALL, cls.SKILLS_WRITE,
            cls.ACL_READ, cls.ACL_WRITE,
            cls.TOKENS_READ, cls.TOKENS_WRITE,
        ]

class APIKeyCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=255)
    scopes: List[str] = Field(..., min_items=1)
    expires_at: Optional[datetime] = None

class APIKeyResponse(BaseModel):
    id: str
    name: str
    key_prefix: str
    scopes: List[str]
    expires_at: Optional[datetime]
    last_used_at: Optional[datetime]
    is_active: bool
    created_at: datetime

class APIKeyCreateResponse(BaseModel):
    key: str  # Full key, only returned once
    api_key: APIKeyResponse

class APIKeyUpdate(BaseModel):
    name: Optional[str] = Field(None, min_length=1, max_length=255)
    scopes: Optional[List[str]] = None
    expires_at: Optional[datetime] = None
    is_active: Optional[bool] = None
```

**Step 2: Write API Key service**

Create `backend/services/api_key_service.py`:

```python
import hashlib
import secrets
from sqlalchemy.orm import Session
from typing import List, Optional
from datetime import datetime, timezone

from backend.models.api_key import APIKey
from backend.models.user import User
from backend.schemas.api_key import APIKeyCreate, APIKeyUpdate
from backend.core.exceptions import ValidationException, NotFoundException

class APIKeyService:
    @staticmethod
    def generate_api_key() -> tuple[str, str, str]:
        """
        Generate API key
        Returns: (full_key, key_hash, key_prefix)
        """
        key = f"sk_{secrets.token_urlsafe(32)}"
        key_hash = hashlib.sha256(key.encode()).hexdigest()
        key_prefix = key[:10]
        return key, key_hash, key_prefix

    @staticmethod
    def create(db: Session, user: User, data: APIKeyCreate) -> tuple[APIKey, str]:
        """Create a new API key"""
        # Validate scopes
        valid_scopes = APIScope.all_scopes()
        invalid_scopes = set(data.scopes) - set(valid_scopes)
        if invalid_scopes:
            raise ValidationException(f"Invalid scopes: {', '.join(invalid_scopes)}")

        # Check max keys limit
        active_count = db.query(APIKey).filter(
            APIKey.user_id == user.id,
            APIKey.is_active == True
        ).count()
        if active_count >= 100:
            raise ValidationException("Maximum 100 active API keys allowed")

        # Generate key
        key, key_hash, key_prefix = APIKeyService.generate_api_key()

        api_key = APIKey(
            user_id=user.id,
            name=data.name,
            key_hash=key_hash,
            key_prefix=key_prefix,
            scopes=data.scopes,
            expires_at=data.expires_at,
        )
        db.add(api_key)
        db.commit()
        db.refresh(api_key)

        return api_key, key

    @staticmethod
    def list_by_user(db: Session, user_id: str, skip: int = 0, limit: int = 100) -> List[APIKey]:
        """List all API keys for a user"""
        return db.query(APIKey).filter(
            APIKey.user_id == user_id
        ).offset(skip).limit(limit).all()

    @staticmethod
    def get_by_id(db: Session, id: str, user_id: str) -> APIKey:
        """Get API key by ID (user must own it)"""
        api_key = db.query(APIKey).filter(
            APIKey.id == id,
            APIKey.user_id == user_id
        ).first()
        if not api_key:
            raise NotFoundException("API key not found")
        return api_key

    @staticmethod
    def update(db: Session, id: str, user_id: str, data: APIKeyUpdate) -> APIKey:
        """Update API key"""
        api_key = APIKeyService.get_by_id(db, id, user_id)

        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            setattr(api_key, field, value)

        db.commit()
        db.refresh(api_key)
        return api_key

    @staticmethod
    def revoke(db: Session, id: str, user_id: str) -> APIKey:
        """Revoke (delete) an API key"""
        api_key = APIKeyService.get_by_id(db, id, user_id)
        api_key.is_active = False
        db.commit()
        return api_key

    @staticmethod
    def rotate(db: Session, id: str, user_id: str) -> tuple[APIKey, str]:
        """Rotate API key - generate new key, keep settings"""
        api_key = APIKeyService.get_by_id(db, id, user_id)

        # Generate new key
        key, key_hash, key_prefix = APIKeyService.generate_api_key()

        # Update but keep name, scopes, expires_at
        api_key.key_hash = key_hash
        api_key.key_prefix = key_prefix

        db.commit()
        db.refresh(api_key)

        return api_key, key

    @staticmethod
    def authenticate(db: Session, key: str) -> Optional[APIKey]:
        """Authenticate API key, return APIKey object if valid"""
        key_hash = hashlib.sha256(key.encode()).hexdigest()

        api_key = db.query(APIKey).filter(
            APIKey.key_hash == key_hash,
            APIKey.is_active == True
        ).first()

        if not api_key:
            return None

        # Check expiration
        if api_key.expires_at and api_key.expires_at < datetime.now(timezone.utc):
            return None

        # Update last_used_at
        api_key.last_used_at = datetime.now(timezone.utc)
        db.commit()

        return api_key

    @staticmethod
    def has_scope(api_key: APIKey, required_scope: str) -> bool:
        """Check if API key has required scope"""
        return required_scope in api_key.scopes
```

**Step 3: Write tests for API Key service**

Create `backend/tests/test_api_key_service.py`:

```python
import pytest
from backend.services.api_key_service import APIKeyService
from backend.schemas.api_key import APIKeyCreate
from backend.models.user import User
from backend.core.exceptions import ValidationException, NotFoundException

def test_create_api_key(db, test_user):
    """Test creating an API key"""
    data = APIKeyCreate(
        name="Test Key",
        scopes=["resources:read", "skills:call"]
    )
    api_key, full_key = APIKeyService.create(db, test_user, data)

    assert api_key.id is not None
    assert api_key.name == "Test Key"
    assert api_key.user_id == test_user.id
    assert api_key.key_prefix == full_key[:10]
    assert full_key.startswith("sk_")
    assert api_key.scopes == ["resources:read", "skills:call"]
    assert api_key.is_active is True

def test_create_api_key_invalid_scopes(db, test_user):
    """Test creating API key with invalid scopes"""
    data = APIKeyCreate(
        name="Test Key",
        scopes=["invalid:scope"]
    )

    with pytest.raises(ValidationException):
        APIKeyService.create(db, test_user, data)

def test_authenticate_api_key(db, test_user):
    """Test authenticating with API key"""
    data = APIKeyCreate(
        name="Test Key",
        scopes=["resources:read"]
    )
    api_key, full_key = APIKeyService.create(db, test_user, data)

    # Authenticate with correct key
    authenticated = APIKeyService.authenticate(db, full_key)
    assert authenticated is not None
    assert authenticated.id == api_key.id

    # Authenticate with wrong key
    authenticated = APIKeyService.authenticate(db, "sk_wrongkey")
    assert authenticated is None

def test_revoke_api_key(db, test_user):
    """Test revoking an API key"""
    data = APIKeyCreate(
        name="Test Key",
        scopes=["resources:read"]
    )
    api_key, _ = APIKeyService.create(db, test_user, data)

    revoked = APIKeyService.revoke(db, api_key.id, test_user.id)
    assert revoked.is_active is False

    # Revoked key should not authenticate
    authenticated = APIKeyService.authenticate(db, "sk_anykey")
    assert authenticated is None
```

**Step 4: Run tests**

Run: `cd backend && pytest tests/test_api_key_service.py -v`
Expected: All tests pass

**Step 5: Commit**

```bash
git add backend/services/api_key_service.py backend/schemas/api_key.py backend/tests/test_api_key_service.py
git commit -m "feat(api-key): implement API key service layer with tests"
```

---

## Task 3: Create API Key API Endpoints

**Files:**
- Create: `backend/api/api_key.py`
- Modify: `backend/api/__init__.py`

**Step 1: Write API key endpoints**

Create `backend/api/api_key.py`:

```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from typing import List

from backend.database import get_db
from backend.models.user import User
from backend.models.api_key import APIKey
from backend.schemas.api_key import (
    APIKeyCreate, APIKeyResponse, APIKeyCreateResponse,
    APIKeyUpdate
)
from backend.services.api_key_service import APIKeyService
from backend.core.dependencies import get_current_active_user
from backend.core.exceptions import ValidationException, NotFoundException

router = APIRouter(prefix="/api/v1/api-keys", tags=["api-keys"])

@router.post("", response_model=APIKeyCreateResponse, status_code=status.HTTP_201_CREATED)
def create_api_key(
    data: APIKeyCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
):
    """Create a new API key"""
    try:
        api_key, full_key = APIKeyService.create(db, current_user, data)
        return APIKeyCreateResponse(
            key=full_key,
            api_key=APIKeyResponse.model_validate(api_key)
        )
    except ValidationException as e:
        raise HTTPException(status_code=400, detail=str(e))

@router.get("", response_model=List[APIKeyResponse])
def list_api_keys(
    skip: int = 0,
    limit: int = 100,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
):
    """List all API keys for current user"""
    api_keys = APIKeyService.list_by_user(db, current_user.id, skip, limit)
    return [APIKeyResponse.model_validate(key) for key in api_keys]

@router.get("/{key_id}", response_model=APIKeyResponse)
def get_api_key(
    key_id: str,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
):
    """Get API key details (does not include full key)"""
    try:
        api_key = APIKeyService.get_by_id(db, key_id, current_user.id)
        return APIKeyResponse.model_validate(api_key)
    except NotFoundException as e:
        raise HTTPException(status_code=404, detail=str(e))

@router.put("/{key_id}", response_model=APIKeyResponse)
def update_api_key(
    key_id: str,
    data: APIKeyUpdate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
):
    """Update API key (name, scopes, expiration, status)"""
    try:
        api_key = APIKeyService.update(db, key_id, current_user.id, data)
        return APIKeyResponse.model_validate(api_key)
    except NotFoundException as e:
        raise HTTPException(status_code=404, detail=str(e))

@router.delete("/{key_id}", response_model=APIKeyResponse)
def revoke_api_key(
    key_id: str,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
):
    """Revoke (deactivate) an API key"""
    try:
        api_key = APIKeyService.revoke(db, key_id, current_user.id)
        return APIKeyResponse.model_validate(api_key)
    except NotFoundException as e:
        raise HTTPException(status_code=404, detail=str(e))

@router.post("/{key_id}/rotate", response_model=APIKeyCreateResponse)
def rotate_api_key(
    key_id: str,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
):
    """Rotate API key - generate new key, keep settings"""
    try:
        api_key, full_key = APIKeyService.rotate(db, key_id, current_user.id)
        return APIKeyCreateResponse(
            key=full_key,
            api_key=APIKeyResponse.model_validate(api_key)
        )
    except NotFoundException as e:
        raise HTTPException(status_code=404, detail=str(e))
```

**Step 2: Register router**

Modify `backend/api/__init__.py`:

```python
from backend.api.api_key import router as api_key_router

# Add to app startup
app.include_router(api_key_router)
```

**Step 3: Write API integration tests**

Create `backend/tests/test_api_key_api.py`:

```python
import pytest
from fastapi.testclient import TestClient

def test_create_api_key(client: TestClient, auth_headers: dict):
    """Test creating API key via API"""
    response = client.post(
        "/api/v1/api-keys",
        json={
            "name": "Test Key",
            "scopes": ["resources:read", "skills:call"]
        },
        headers=auth_headers
    )
    assert response.status_code == 201
    data = response.json()
    assert "key" in data
    assert data["key"].startswith("sk_")
    assert data["api_key"]["name"] == "Test Key"
    assert data["api_key"]["key_prefix"] == data["key"][:10]

def test_list_api_keys(client: TestClient, auth_headers: dict):
    """Test listing API keys"""
    response = client.get("/api/v1/api-keys", headers=auth_headers)
    assert response.status_code == 200
    assert isinstance(response.json(), list)

def test_revoke_api_key(client: TestClient, auth_headers: dict):
    """Test revoking API key"""
    # Create key first
    create_response = client.post(
        "/api/v1/api-keys",
        json={"name": "Test Key", "scopes": ["resources:read"]},
        headers=auth_headers
    )
    key_id = create_response.json()["api_key"]["id"]

    # Revoke it
    response = client.delete(f"/api/v1/api-keys/{key_id}", headers=auth_headers)
    assert response.status_code == 200
    assert response.json()["is_active"] is False
```

**Step 4: Run tests**

Run: `cd backend && pytest tests/test_api_key_api.py -v`
Expected: All tests pass

**Step 5: Commit**

```bash
git add backend/api/api_key.py backend/api/__init__.py backend/tests/test_api_key_api.py
git commit -m "feat(api-key): add API key endpoints with integration tests"
```

---

## Task 4: Create API Key Authentication Middleware

**Files:**
- Create: `backend/middleware/api_key_auth.py`
- Modify: `backend/core/dependencies.py`

**Step 1: Write API key authentication middleware**

Create `backend/middleware/api_key_auth.py`:

```python
from fastapi import Request, HTTPException, status
from sqlalchemy.orm import Session
from typing import Optional

from backend.database import get_db
from backend.services.api_key_service import APIKeyService
from backend.models.user import User
from backend.models.api_key import APIKey

async def api_key_middleware(request: Request, call_next):
    """
    Middleware to authenticate API keys from Authorization header
    Format: Authorization: Bearer sk_xxx
    """
    auth_header = request.headers.get("Authorization")

    if auth_header and auth_header.startswith("Bearer sk_"):
        key = auth_header[7:]  # Remove "Bearer "

        # Get DB session
        db: Session = next(get_db())

        # Authenticate key
        api_key: Optional[APIKey] = APIKeyService.authenticate(db, key)

        if api_key:
            # Valid key - attach to request state
            request.state.user_id = api_key.user_id
            request.state.api_key_id = api_key.id
            request.state.auth_method = "api_key"
            request.state.api_key_scopes = api_key.scopes
        else:
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="Invalid or expired API key"
            )

    response = await call_next(request)
    return response
```

**Step 2: Update dependencies to support API key auth**

Modify `backend/core/dependencies.py`:

```python
from fastapi import Request, Depends, HTTPException, status
from sqlalchemy.orm import Session
from backend.database import get_db
from backend.models.user import User
from backend.models.api_key import APIKey
from backend.services.api_key_service import APIKeyService

async def get_current_user_optional(
    request: Request,
    db: Session = Depends(get_db)
) -> Optional[User]:
    """
    Get current user from JWT or API key (optional - returns None if not authenticated)
    """
    # Check if authenticated by API key
    if hasattr(request.state, "user_id"):
        user = db.query(User).filter(User.id == request.state.user_id).first()
        return user

    # Check JWT token (existing logic)
    # ... existing JWT code ...

    return None

def require_scope(required_scope: str):
    """Dependency to check if API key has required scope"""
    def scope_checker(request: Request) -> bool:
        if request.state.auth_method == "api_key":
            scopes = request.state.api_key_scopes
            if required_scope not in scopes:
                raise HTTPException(
                    status_code=status.HTTP_403_FORBIDDEN,
                    detail=f"API key missing required scope: {required_scope}"
                )
        return True
    return scope_checker
```

**Step 3: Register middleware in main app**

Modify `backend/app.py`:

```python
from backend.middleware.api_key_auth import api_key_middleware

app.add_middleware(api_key_middleware)
```

**Step 4: Write middleware tests**

Create `backend/tests/test_api_key_middleware.py`:

```python
import pytest
from fastapi.testclient import TestClient

def test_api_key_auth_success(client: TestClient, test_api_key: str):
    """Test successful API key authentication"""
    response = client.get(
        "/api/v1/resources",
        headers={"Authorization": f"Bearer {test_api_key}"}
    )
    assert response.status_code == 200

def test_api_key_auth_invalid(client: TestClient):
    """Test invalid API key"""
    response = client.get(
        "/api/v1/resources",
        headers={"Authorization": "Bearer sk_invalidkey"}
    )
    assert response.status_code == 401

def test_api_key_scope_check(client: TestClient, test_api_key: str):
    """Test scope checking"""
    # This would require an endpoint with scope requirement
    pass
```

**Step 5: Run tests**

Run: `cd backend && pytest tests/test_api_key_middleware.py -v`
Expected: All tests pass

**Step 6: Commit**

```bash
git add backend/middleware/api_key_auth.py backend/core/dependencies.py backend/app.py backend/tests/test_api_key_middleware.py
git commit -m "feat(api-key): add API key authentication middleware"
```

---

## Task 5: Create Audit Log Database Model

**Files:**
- Create: `backend/models/audit_log.py`
- Create: `backend/alembic/versions/xxx_create_audit_logs_table.py`

**Step 1: Write Audit Log model**

Create `backend/models/audit_log.py`:

```python
from sqlalchemy import Column, String, DateTime, Text, ForeignKey, JSON, Boolean
from sqlalchemy.orm import relationship
from datetime import datetime, timezone
import uuid
from backend.models.base import Base

class AuditLog(Base):
    """Comprehensive audit log for all system actions"""
    __tablename__ = "audit_logs"

    id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))
    user_id = Column(String(36), ForeignKey("users.id", ondelete="SET NULL"), nullable=True, index=True)
    action = Column(String(100), nullable=False, index=True)
    resource_type = Column(String(50), nullable=True, index=True)
    resource_id = Column(String(36), nullable=True)
    details = Column(JSON, nullable=True)
    ip_address = Column(String(45), nullable=True)
    user_agent = Column(String(500), nullable=True)
    status = Column(String(20), nullable=False, default="success")
    error_message = Column(Text, nullable=True)
    created_at = Column(DateTime(timezone=True), default=lambda: datetime.now(timezone.utc), nullable=False, index=True)

    user = relationship("User", back_populates="audit_logs")

    def __repr__(self):
        return f"<AuditLog(id={self.id}, action={self.action}, status={self.status})>"
```

**Step 2: Update User model**

Modify `backend/models/user.py`, add relationship:

```python
audit_logs = relationship("AuditLog", back_populates="user")
```

**Step 3: Create migration**

Create `backend/alembic/versions/002_create_audit_logs_table.py`:

```python
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = '002_create_audit_logs'
down_revision = '001_create_api_keys'
branch_labels = None
depends_on = None

def upgrade():
    op.create_table(
        'audit_logs',
        sa.Column('id', sa.String(36), primary_key=True),
        sa.Column('user_id', sa.String(36), sa.ForeignKey('users.id', ondelete='SET NULL'), nullable=True),
        sa.Column('action', sa.String(100), nullable=False),
        sa.Column('resource_type', sa.String(50), nullable=True),
        sa.Column('resource_id', sa.String(36), nullable=True),
        sa.Column('details', postgresql.JSON(), nullable=True),
        sa.Column('ip_address', sa.String(45), nullable=True),
        sa.Column('user_agent', sa.String(500), nullable=True),
        sa.Column('status', sa.String(20), nullable=False, server_default='success'),
        sa.Column('error_message', sa.Text(), nullable=True),
        sa.Column('created_at', sa.DateTime(timezone=True), nullable=False, server_default=sa.text('now()')),
        sa.Index('ix_audit_logs_user_id', 'user_id'),
        sa.Index('ix_audit_logs_action', 'action'),
        sa.Index('ix_audit_logs_created_at', 'created_at'),
    )

def downgrade():
    op.drop_table('audit_logs')
```

**Step 4: Run migration**

Run: `cd backend && alembic upgrade head`
Expected: Migration applied successfully

**Step 5: Commit**

```bash
git add backend/models/audit_log.py backend/models/user.py backend/alembic/versions/002_create_audit_logs_table.py
git commit -m "feat(audit): add audit log database model and migration"
```

---

## Task 6: Create Audit Log Service and Middleware

**Files:**
- Create: `backend/services/audit_log_service.py`
- Create: `backend/middleware/audit_middleware.py`
- Create: `backend/schemas/audit_log.py`

**Step 1: Write Audit Log schemas**

Create `backend/schemas/audit_log.py`:

```python
from pydantic import BaseModel
from datetime import datetime
from typing import Optional, Any
from enum import Enum

class AuditAction(str, Enum):
    LOGIN = "login"
    LOGOUT = "logout"
    LOGIN_FAILED = "login_failed"
    RESOURCE_CREATE = "resource.create"
    RESOURCE_UPDATE = "resource.update"
    RESOURCE_DELETE = "resource.delete"
    RESOURCE_READ = "resource.read"
    SKILL_CALL = "skill.call"
    SKILL_CREATE = "skill.create"
    SKILL_UPDATE = "skill.update"
    SKILL_DELETE = "skill.delete"
    ACL_CREATE = "acl.create"
    ACL_UPDATE = "acl.update"
    ACL_DELETE = "acl.delete"
    PERMISSION_CHECK = "acl.check_permission"
    API_KEY_CREATE = "api_key.create"
    API_KEY_DELETE = "api_key.delete"
    API_KEY_USE = "api_key.use"
    USER_CREATE = "user.create"
    USER_UPDATE = "user.update"
    USER_DELETE = "user.delete"
    ROLE_ASSIGN = "user.role_assign"

class AuditLogResponse(BaseModel):
    id: str
    user_id: Optional[str]
    action: str
    resource_type: Optional[str]
    resource_id: Optional[str]
    details: Optional[Any]
    ip_address: Optional[str]
    user_agent: Optional[str]
    status: str
    error_message: Optional[str]
    created_at: datetime

class AuditLogListResponse(BaseModel):
    items: list[AuditLogResponse]
    total: int
    page: int
    size: int
```

**Step 2: Write Audit Log service**

Create `backend/services/audit_log_service.py`:

```python
from sqlalchemy.orm import Session
from typing import Optional, Any, List
from datetime import datetime

from backend.models.audit_log import AuditLog
from backend.models.user import User

class AuditLogService:
    @staticmethod
    def log_action(
        db: Session,
        user_id: Optional[str],
        action: str,
        resource_type: Optional[str] = None,
        resource_id: Optional[str] = None,
        details: Optional[Any] = None,
        ip_address: Optional[str] = None,
        user_agent: Optional[str] = None,
        status: str = "success",
        error_message: Optional[str] = None
    ) -> AuditLog:
        """Log an audit action"""
        log = AuditLog(
            user_id=user_id,
            action=action,
            resource_type=resource_type,
            resource_id=resource_id,
            details=details,
            ip_address=ip_address,
            user_agent=user_agent,
            status=status,
            error_message=error_message
        )
        db.add(log)
        db.commit()
        db.refresh(log)
        return log

    @staticmethod
    def list_logs(
        db: Session,
        skip: int = 0,
        limit: int = 100,
        action: Optional[str] = None,
        resource_type: Optional[str] = None,
        user_id: Optional[str] = None,
        start_date: Optional[datetime] = None,
        end_date: Optional[datetime] = None
    ) -> tuple[List[AuditLog], int]:
        """List audit logs with filters"""
        query = db.query(AuditLog)

        if action:
            query = query.filter(AuditLog.action == action)
        if resource_type:
            query = query.filter(AuditLog.resource_type == resource_type)
        if user_id:
            query = query.filter(AuditLog.user_id == user_id)
        if start_date:
            query = query.filter(AuditLog.created_at >= start_date)
        if end_date:
            query = query.filter(AuditLog.created_at <= end_date)

        total = query.count()
        logs = query.order_by(AuditLog.created_at.desc()).offset(skip).limit(limit).all()

        return logs, total
```

**Step 3: Write audit middleware**

Create `backend/middleware/audit_middleware.py`:

```python
from fastapi import Request
from sqlalchemy.orm import Session
from time import time
import os

from backend.database import get_db
from backend.services.audit_log_service import AuditLogService

AUDIT_LOG_ENABLED = os.getenv("AUDIT_LOG_ENABLED", "true").lower() == "true"
SENSITIVE_FIELDS = os.getenv("AUDIT_LOG_SENSITIVE_FIELDS", "password,token,secret").split(",")

async def audit_middleware(request: Request, call_next):
    """Log all HTTP requests to audit logs"""
    if not AUDIT_LOG_ENABLED:
        return await call_next(request)

    start_time = time()

    # Extract info
    user_id = getattr(request.state, "user_id", None)
    ip_address = request.client.host if request.client else None
    user_agent = request.headers.get("user-agent")

    # Process request
    response = await call_next(request)

    # Calculate duration
    duration = time() - start_time

    # Determine action from method + path
    action = f"{request.method.lower()} {request.url.path}"

    # Log the request
    db: Session = next(get_db())

    AuditLogService.log_action(
        db=db,
        user_id=user_id,
        action=action,
        resource_type="http_request",
        details={
            "method": request.method,
            "path": request.url.path,
            "query_params": dict(request.query_params),
            "status_code": response.status_code,
            "duration_ms": round(duration * 1000, 2)
        },
        ip_address=ip_address,
        user_agent=user_agent,
        status="success" if response.status_code < 400 else "failure"
    )

    return response
```

**Step 4: Register middleware**

Modify `backend/app.py`:

```python
from backend.middleware.audit_middleware import audit_middleware

app.add_middleware(audit_middleware)
```

**Step 5: Write tests**

Create `backend/tests/test_audit_log_service.py`:

```python
import pytest
from backend.services.audit_log_service import AuditLogService

def test_log_action(db, test_user):
    """Test logging an action"""
    log = AuditLogService.log_action(
        db=db,
        user_id=test_user.id,
        action="test.action",
        resource_type="test",
        resource_id="test-123",
        details={"key": "value"},
        ip_address="127.0.0.1",
        user_agent="test-agent"
    )

    assert log.id is not None
    assert log.action == "test.action"
    assert log.status == "success"

def test_list_logs_with_filters(db, test_user):
    """Test listing logs with filters"""
    # Create some test logs
    AuditLogService.log_action(db, test_user.id, "action1")
    AuditLogService.log_action(db, test_user.id, "action2")

    logs, total = AuditLogService.list_logs(
        db,
        action="action1",
        user_id=test_user.id
    )

    assert total == 1
    assert logs[0].action == "action1"
```

**Step 6: Run tests**

Run: `cd backend && pytest tests/test_audit_log_service.py -v`
Expected: All tests pass

**Step 7: Commit**

```bash
git add backend/services/audit_log_service.py backend/middleware/audit_middleware.py backend/schemas/audit_log.py backend/app.py backend/tests/test_audit_log_service.py
git commit -m "feat(audit): add audit log service and middleware"
```

---

## Task 7: Create Audit Log API Endpoints

**Files:**
- Create: `backend/api/audit_log.py`

**Step 1: Write audit log endpoints**

Create `backend/api/audit_log.py`:

```python
from fastapi import APIRouter, Depends, Query
from sqlalchemy.orm import Session
from typing import Optional
from datetime import datetime

from backend.database import get_db
from backend.models.user import User
from backend.schemas.audit_log import AuditLogResponse, AuditLogListResponse
from backend.services.audit_log_service import AuditLogService
from backend.core.dependencies import get_current_active_user
from backend.core.exceptions import PermissionException

router = APIRouter(prefix="/api/v1/audit-logs", tags=["audit-logs"])

@router.get("", response_model=AuditLogListResponse)
def list_audit_logs(
    page: int = Query(1, ge=1),
    size: int = Query(20, ge=1, le=100),
    action: Optional[str] = None,
    resource_type: Optional[str] = None,
    user_id: Optional[str] = None,
    start_date: Optional[datetime] = None,
    end_date: Optional[datetime] = None,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
):
    """
    List audit logs (admin only)
    Requires admin role
    """
    # Check if user is admin
    if not any(role.name == "admin" for role in current_user.roles):
        raise PermissionException("Admin access required")

    skip = (page - 1) * size
    logs, total = AuditLogService.list_logs(
        db,
        skip=skip,
        limit=size,
        action=action,
        resource_type=resource_type,
        user_id=user_id,
        start_date=start_date,
        end_date=end_date
    )

    return AuditLogListResponse(
        items=[AuditLogResponse.model_validate(log) for log in logs],
        total=total,
        page=page,
        size=size
    )

@router.get("/{log_id}", response_model=AuditLogResponse)
def get_audit_log(
    log_id: str,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user)
):
    """Get audit log details (admin only)"""
    if not any(role.name == "admin" for role in current_user.roles):
        raise PermissionException("Admin access required")

    # Implement get by ID logic
    pass
```

**Step 2: Register router**

Modify `backend/api/__init__.py`:

```python
from backend.api.audit_log import router as audit_log_router

app.include_router(audit_log_router)
```

**Step 3: Commit**

```bash
git add backend/api/audit_log.py backend/api/__init__.py
git commit -m "feat(audit): add audit log API endpoints"
```

---

## Task 8: Create Frontend Settings Page Structure

**Files:**
- Create: `frontend/src/pages/settings/index.ts`
- Create: `frontend/src/pages/settings/SettingsPage.tsx`
- Modify: `frontend/src/App.tsx` or router config

**Step 1: Create Settings page directory and index**

Create `frontend/src/pages/settings/index.ts`:

```typescript
export { SettingsPage } from './SettingsPage';
export { default as APIKeysPage } from './APIKeysPage';
export { default as AuditLogsPage } from './AuditLogsPage';
```

**Step 2: Create Settings page component**

Create `frontend/src/pages/settings/SettingsPage.tsx`:

```typescript
import React from 'react';
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
import { APIKeysPage } from './APIKeysPage';
import { AuditLogsPage } from './AuditLogsPage';

export const SettingsPage: React.FC = () => {
  return (
    <div className="container mx-auto py-8 space-y-6">
      <div>
        <h1 className="text-3xl font-bold">Settings</h1>
        <p className="text-muted-foreground mt-2">
          Manage your API keys and view audit logs
        </p>
      </div>

      <Tabs defaultValue="api-keys" className="space-y-4">
        <TabsList>
          <TabsTrigger value="api-keys">API Keys</TabsTrigger>
          <TabsTrigger value="audit-logs">Audit Logs</TabsTrigger>
        </TabsList>

        <TabsContent value="api-keys">
          <APIKeysPage />
        </TabsContent>

        <TabsContent value="audit-logs">
          <AuditLogsPage />
        </TabsContent>
      </Tabs>
    </div>
  );
};
```

**Step 3: Add Settings route**

Modify your router configuration (typically in `frontend/src/App.tsx` or router file):

```typescript
import { SettingsPage } from './pages/settings';

// Add to routes
<Route path="/settings" element={<SettingsPage />} />
```

**Step 4: Update navigation**

Modify your sidebar/nav component to add Settings link:

```typescript
// Add to navigation items
{
  title: 'Settings',
  href: '/settings',
  icon: Settings,
}
```

**Step 5: Commit**

```bash
git add frontend/src/pages/settings/
git commit -m "feat(settings): create Settings page structure with tabs"
```

---

## Task 9: Create API Keys Frontend Page

**Files:**
- Create: `frontend/src/pages/settings/APIKeysPage.tsx`
- Create: `frontend/src/pages/settings/components/APIKeyFormModal.tsx`
- Create: `frontend/src/pages/settings/components/APIKeyDisplayModal.tsx`
- Create: `frontend/src/hooks/use-api-keys.ts`

**Step 1: Create API keys hooks**

Create `frontend/src/hooks/use-api-keys.ts`:

```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { apiClient } from '@/lib/api-client';

interface APIKey {
  id: string;
  name: string;
  key_prefix: string;
  scopes: string[];
  expires_at: string | null;
  last_used_at: string | null;
  is_active: boolean;
  created_at: string;
}

interface APIKeyCreateResponse {
  key: string;
  api_key: APIKey;
}

export const useAPIKeys = () => {
  return useQuery({
    queryKey: ['api-keys'],
    queryFn: () => apiClient.get<APIKey[]>('/api/v1/api-keys'),
  });
};

export const useCreateAPIKey = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (data: { name: string; scopes: string[]; expires_at?: string }) =>
      apiClient.post<APIKeyCreateResponse>('/api/v1/api-keys', data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['api-keys'] });
    },
  });
};

export const useRevokeAPIKey = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: string) =>
      apiClient.delete<APIKey>(`/api/v1/api-keys/${id}`),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['api-keys'] });
    },
  });
};

export const useRotateAPIKey = () => {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: (id: string) =>
      apiClient.post<APIKeyCreateResponse>(`/api/v1/api-keys/${id}/rotate`),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['api-keys'] });
    },
  });
};
```

**Step 2: Create API Keys page**

Create `frontend/src/pages/settings/APIKeysPage.tsx`:

```typescript
import React, { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Plus } from 'lucide-react';
import { useAPIKeys, useCreateAPIKey, useRevokeAPIKey } from '@/hooks/use-api-keys';
import { APIKeyTable } from './components/APIKeyTable';
import { APIKeyFormModal } from './components/APIKeyFormModal';
import { APIKeyDisplayModal } from './components/APIKeyDisplayModal';

export const APIKeysPage = () => {
  const { data: apiKeys, isLoading } = useAPIKeys();
  const createMutation = useCreateAPIKey();
  const revokeMutation = useRevokeAPIKey();

  const [isFormModalOpen, setIsFormModalOpen] = useState(false);
  const [displayKey, setDisplayKey] = useState<string | null>(null);

  const handleCreateKey = (data: { name: string; scopes: string[] }) => {
    createMutation.mutate(data, {
      onSuccess: (response) => {
        setDisplayKey(response.key);
        setIsFormModalOpen(false);
      },
    });
  };

  if (isLoading) {
    return <div>Loading...</div>;
  }

  return (
    <div className="space-y-6">
      <div className="flex justify-between items-center">
        <div>
          <h2 className="text-2xl font-bold">API Keys</h2>
          <p className="text-muted-foreground">
            Manage your personal API keys for programmatic access
          </p>
        </div>
        <Button onClick={() => setIsFormModalOpen(true)}>
          <Plus className="w-4 h-4 mr-2" />
          Create New Key
        </Button>
      </div>

      <Card>
        <CardHeader>
          <CardTitle>Active Keys</CardTitle>
          <CardDescription>
            {apiKeys?.filter(k => k.is_active).length || 0} active API keys
          </CardDescription>
        </CardHeader>
        <CardContent>
          <APIKeyTable
            apiKeys={apiKeys || []}
            onRevoke={(id) => revokeMutation.mutate(id)}
          />
        </CardContent>
      </Card>

      <APIKeyFormModal
        open={isFormModalOpen}
        onOpenChange={setIsFormModalOpen}
        onSubmit={handleCreateKey}
      />

      {displayKey && (
        <APIKeyDisplayModal
          apiKey={displayKey}
          onClose={() => setDisplayKey(null)}
        />
      )}
    </div>
  );
};

export default APIKeysPage;
```

**Step 3: Create API Key Table component**

Create `frontend/src/pages/settings/components/APIKeyTable.tsx`:

```typescript
import React from 'react';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Trash2 } from 'lucide-react';

interface APIKey {
  id: string;
  name: string;
  key_prefix: string;
  scopes: string[];
  expires_at: string | null;
  last_used_at: string | null;
  is_active: boolean;
  created_at: string;
}

interface Props {
  apiKeys: APIKey[];
  onRevoke: (id: string) => void;
}

export const APIKeyTable: React.FC<Props> = ({ apiKeys, onRevoke }) => {
  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>Name</TableHead>
          <TableHead>Key Prefix</TableHead>
          <TableHead>Scopes</TableHead>
          <TableHead>Expires</TableHead>
          <TableHead>Last Used</TableHead>
          <TableHead>Status</TableHead>
          <TableHead>Actions</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {apiKeys.map((key) => (
          <TableRow key={key.id}>
            <TableCell className="font-medium">{key.name}</TableCell>
            <TableCell className="font-mono text-sm">{key.key_prefix}...</TableCell>
            <TableCell>
              <div className="flex gap-1 flex-wrap">
                {key.scopes.map((scope) => (
                  <Badge key={scope} variant="outline" className="text-xs">
                    {scope}
                  </Badge>
                ))}
              </div>
            </TableCell>
            <TableCell>
              {key.expires_at
                ? new Date(key.expires_at).toLocaleDateString()
                : 'Never'}
            </TableCell>
            <TableCell>
              {key.last_used_at
                ? new Date(key.last_used_at).toLocaleDateString()
                : 'Never'}
            </TableCell>
            <TableCell>
              <Badge variant={key.is_active ? 'success' : 'secondary'}>
                {key.is_active ? 'Active' : 'Revoked'}
              </Badge>
            </TableCell>
            <TableCell>
              {key.is_active && (
                <Button
                  variant="ghost"
                  size="sm"
                  onClick={() => onRevoke(key.id)}
                >
                  <Trash2 className="w-4 h-4" />
                </Button>
              )}
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  );
};
```

**Step 4: Create API Key Form Modal**

Create `frontend/src/pages/settings/components/APIKeyFormModal.tsx`:

```typescript
import React, { useState } from 'react';
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Checkbox } from '@/components/ui/checkbox';

interface Props {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onSubmit: (data: { name: string; scopes: string[] }) => void;
}

const AVAILABLE_SCOPES = [
  { value: 'resources:read', label: 'Resources - Read' },
  { value: 'resources:write', label: 'Resources - Write' },
  { value: 'skills:read', label: 'Skills - Read' },
  { value: 'skills:call', label: 'Skills - Call' },
  { value: 'skills:write', label: 'Skills - Write' },
  { value: 'acl:read', label: 'ACL - Read' },
  { value: 'acl:write', label: 'ACL - Write' },
];

export const APIKeyFormModal: React.FC<Props> = ({ open, onOpenChange, onSubmit }) => {
  const [name, setName] = useState('');
  const [scopes, setScopes] = useState<string[]>([]);

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (name.trim() && scopes.length > 0) {
      onSubmit({ name, scopes });
      setName('');
      setScopes([]);
    }
  };

  const toggleScope = (scope: string) => {
    setScopes(prev =>
      prev.includes(scope)
        ? prev.filter(s => s !== scope)
        : [...prev, scope]
    );
  };

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Create API Key</DialogTitle>
          <DialogDescription>
            Create a new API key for programmatic access. Choose which permissions to grant.
          </DialogDescription>
        </DialogHeader>

        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <Label htmlFor="name">Name</Label>
            <Input
              id="name"
              value={name}
              onChange={(e) => setName(e.target.value)}
              placeholder="e.g., Production API Key"
              required
            />
          </div>

          <div>
            <Label>Permissions</Label>
            <div className="space-y-2 mt-2">
              {AVAILABLE_SCOPES.map((scope) => (
                <div key={scope.value} className="flex items-center space-x-2">
                  <Checkbox
                    id={scope.value}
                    checked={scopes.includes(scope.value)}
                    onCheckedChange={() => toggleScope(scope.value)}
                  />
                  <Label htmlFor={scope.value} className="text-sm">
                    {scope.label}
                  </Label>
                </div>
              ))}
            </div>
          </div>

          <div className="flex justify-end space-x-2">
            <Button type="button" variant="outline" onClick={() => onOpenChange(false)}>
              Cancel
            </Button>
            <Button type="submit" disabled={!name.trim() || scopes.length === 0}>
              Create Key
            </Button>
          </div>
        </form>
      </DialogContent>
    </Dialog>
  );
};
```

**Step 5: Create API Key Display Modal**

Create `frontend/src/pages/settings/components/APIKeyDisplayModal.tsx`:

```typescript
import React from 'react';
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { Copy, Check } from 'lucide-react';
import { useState } from 'react';

interface Props {
  apiKey: string;
  onClose: () => void;
}

export const APIKeyDisplayModal: React.FC<Props> = ({ apiKey, onClose }) => {
  const [copied, setCopied] = useState(false);

  const handleCopy = async () => {
    await navigator.clipboard.writeText(apiKey);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };

  return (
    <Dialog open={!!apiKey} onOpenChange={() => onClose()}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>API Key Created</DialogTitle>
          <DialogDescription>
            Copy this key now. You won't be able to see it again!
          </DialogDescription>
        </DialogHeader>

        <div className="space-y-4">
          <div className="bg-muted p-4 rounded-md">
            <code className="text-sm break-all">{apiKey}</code>
          </div>

          <Button onClick={handleCopy} className="w-full">
            {copied ? (
              <>
                <Check className="w-4 h-4 mr-2" />
                Copied!
              </>
            ) : (
              <>
                <Copy className="w-4 h-4 mr-2" />
                Copy Key
              </>
            )}
          </Button>

          <p className="text-sm text-muted-foreground">
            Store this key securely. Treat it like a password.
          </p>
        </div>

        <div className="flex justify-end">
          <Button onClick={onClose}>Done</Button>
        </div>
      </DialogContent>
    </Dialog>
  );
};
```

**Step 6: Commit**

```bash
git add frontend/src/pages/settings/APIKeysPage.tsx frontend/src/pages/settings/components/ frontend/src/hooks/use-api-keys.ts
git commit -m "feat(settings): implement API Keys page with create and manage functionality"
```

---

## Task 10: Create Audit Logs Frontend Page

**Files:**
- Create: `frontend/src/pages/settings/AuditLogsPage.tsx`
- Create: `frontend/src/pages/settings/components/AuditLogTable.tsx`
- Create: `frontend/src/hooks/use-audit-logs.ts`

**Step 1: Create audit logs hooks**

Create `frontend/src/hooks/use-audit-logs.ts`:

```typescript
import { useQuery } from '@tanstack/react-query';
import { apiClient } from '@/lib/api-client';

interface AuditLog {
  id: string;
  user_id: string | null;
  action: string;
  resource_type: string | null;
  resource_id: string | null;
  details: any;
  ip_address: string | null;
  user_agent: string | null;
  status: string;
  error_message: string | null;
  created_at: string;
}

interface AuditLogsResponse {
  items: AuditLog[];
  total: number;
  page: number;
  size: number;
}

export const useAuditLogs = (filters: {
  page?: number;
  size?: number;
  action?: string;
  resource_type?: string;
  user_id?: string;
  start_date?: string;
  end_date?: string;
} = {}) => {
  return useQuery({
    queryKey: ['audit-logs', filters],
    queryFn: () => apiClient.get<AuditLogsResponse>('/api/v1/audit-logs', { params: filters }),
  });
};
```

**Step 2: Create Audit Logs page**

Create `frontend/src/pages/settings/AuditLogsPage.tsx`:

```typescript
import React, { useState } from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { AuditLogTable } from './components/AuditLogTable';
import { AuditLogFilters } from './components/AuditLogFilters';
import { useAuditLogs } from '@/hooks/use-audit-logs';

export const AuditLogsPage = () => {
  const [filters, setFilters] = useState({
    page: 1,
    size: 20,
    action: '',
    resource_type: '',
  });

  const { data, isLoading } = useAuditLogs(filters);

  const handleFilterChange = (newFilters: Partial<typeof filters>) => {
    setFilters(prev => ({ ...prev, ...newFilters, page: 1 }));
  };

  const handlePageChange = (page: number) => {
    setFilters(prev => ({ ...prev, page }));
  };

  return (
    <div className="space-y-6">
      <div>
        <h2 className="text-2xl font-bold">Audit Logs</h2>
        <p className="text-muted-foreground">
          Comprehensive audit trail of all system actions
        </p>
      </div>

      <AuditLogFilters filters={filters} onFilterChange={handleFilterChange} />

      <Card>
        <CardHeader>
          <CardTitle>Activity Log</CardTitle>
        </CardHeader>
        <CardContent>
          <AuditLogTable
            logs={data?.items || []}
            total={data?.total || 0}
            page={filters.page}
            size={filters.size}
            onPageChange={handlePageChange}
            isLoading={isLoading}
          />
        </CardContent>
      </Card>
    </div>
  );
};

export default AuditLogsPage;
```

**Step 3: Create Audit Log Filters component**

Create `frontend/src/pages/settings/components/AuditLogFilters.tsx`:

```typescript
import React from 'react';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';

interface Props {
  filters: any;
  onFilterChange: (filters: any) => void;
}

export const AuditLogFilters: React.FC<Props> = ({ filters, onFilterChange }) => {
  return (
    <Card>
      <CardContent className="pt-6">
        <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
          <div>
            <Label>Search</Label>
            <Input
              placeholder="Search..."
              value={filters.search || ''}
              onChange={(e) => onFilterChange({ search: e.target.value })}
            />
          </div>

          <div>
            <Label>Action</Label>
            <Input
              placeholder="Filter by action"
              value={filters.action || ''}
              onChange={(e) => onFilterChange({ action: e.target.value })}
            />
          </div>

          <div>
            <Label>Resource Type</Label>
            <Input
              placeholder="Filter by resource"
              value={filters.resource_type || ''}
              onChange={(e) => onFilterChange({ resource_type: e.target.value })}
            />
          </div>

          <div>
            <Label>Page Size</Label>
            <select
              className="w-full h-10 rounded-md border border-input bg-background px-3"
              value={filters.size}
              onChange={(e) => onFilterChange({ size: Number(e.target.value) })}
            >
              <option value={20}>20 per page</option>
              <option value={50}>50 per page</option>
              <option value={100}>100 per page</option>
            </select>
          </div>
        </div>
      </CardContent>
    </Card>
  );
};
```

**Step 4: Create Audit Log Table component**

Create `frontend/src/pages/settings/components/AuditLogTable.tsx`:

```typescript
import React from 'react';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { ChevronLeft, ChevronRight } from 'lucide-react';

interface AuditLog {
  id: string;
  user_id: string | null;
  action: string;
  resource_type: string | null;
  status: string;
  created_at: string;
}

interface Props {
  logs: AuditLog[];
  total: number;
  page: number;
  size: number;
  onPageChange: (page: number) => void;
  isLoading: boolean;
}

export const AuditLogTable: React.FC<Props> = ({
  logs,
  total,
  page,
  size,
  onPageChange,
  isLoading
}) => {
  const totalPages = Math.ceil(total / size);

  if (isLoading) {
    return <div className="text-center py-8">Loading...</div>;
  }

  return (
    <div className="space-y-4">
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead>Time</TableHead>
            <TableHead>Action</TableHead>
            <TableHead>Resource Type</TableHead>
            <TableHead>Status</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {logs.map((log) => (
            <TableRow key={log.id}>
              <TableCell className="text-sm">
                {new Date(log.created_at).toLocaleString()}
              </TableCell>
              <TableCell className="font-mono text-xs">
                {log.action}
              </TableCell>
              <TableCell>
                {log.resource_type && (
                  <Badge variant="outline">{log.resource_type}</Badge>
                )}
              </TableCell>
              <TableCell>
                <Badge variant={log.status === 'success' ? 'success' : 'destructive'}>
                  {log.status}
                </Badge>
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>

      {totalPages > 1 && (
        <div className="flex justify-center items-center space-x-2">
          <Button
            variant="outline"
            size="sm"
            onClick={() => onPageChange(page - 1)}
            disabled={page === 1}
          >
            <ChevronLeft className="w-4 h-4" />
          </Button>
          <span className="text-sm">
            Page {page} of {totalPages}
          </span>
          <Button
            variant="outline"
            size="sm"
            onClick={() => onPageChange(page + 1)}
            disabled={page === totalPages}
          >
            <ChevronRight className="w-4 h-4" />
          </Button>
        </div>
      )}
    </div>
  );
};
```

**Step 5: Commit**

```bash
git add frontend/src/pages/settings/AuditLogsPage.tsx frontend/src/pages/settings/components/AuditLog*.tsx frontend/src/hooks/use-audit-logs.ts
git commit -m "feat(settings): implement Audit Logs page with filters and pagination"
```

---

## Task 11: Remove API Keys and Audit Logs from ACL Page

**Files:**
- Modify: `frontend/src/pages/acl/ACLPage.tsx`

**Step 1: Update ACL page to remove tabs**

Modify `frontend/src/pages/acl/ACLPage.tsx`, remove API Keys and Audit Logs tabs if they exist:

```typescript
// Remove these tabs if present
// - <TabsTrigger value="api-keys">API Keys</TabsTrigger>
// - <TabsTrigger value="audit-logs">Audit Logs</TabsTrigger>
// - <TabsContent value="api-keys">...</TabsContent>
// - <TabsContent value="audit-logs">...</TabsContent>
```

**Step 2: Commit**

```bash
git add frontend/src/pages/acl/ACLPage.tsx
git commit -m "refactor(acl): remove API Keys and Audit Logs tabs (moved to Settings)"
```

---

## Task 12: Add Environment Configuration

**Files:**
- Modify: `backend/.env.example`
- Modify: `backend/config.py` or similar

**Step 1: Update environment variables**

Add to `backend/.env.example`:

```bash
# API Keys
API_KEY_MAX_PER_USER=100
API_KEY_DEFAULT_EXPIRATION_DAYS=365
API_KEY_PREFIX=sk_

# Audit Logs
AUDIT_LOG_ENABLED=true
AUDIT_LOG_RETENTION_DAYS=90
AUDIT_LOG_ASYNC=true
AUDIT_LOG_SENSITIVE_FIELDS=password,token,secret
```

**Step 2: Commit**

```bash
git add backend/.env.example
git commit -m "feat(config): add environment variables for API keys and audit logs"
```

---

## Task 13: End-to-End Testing

**Files:**
- Create: `frontend/e2e/api-keys.spec.ts`
- Create: `frontend/e2e/audit-logs.spec.ts`

**Step 1: Create API Keys E2E test**

Create `frontend/e2e/api-keys.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('API Keys', () => {
  test.beforeEach(async ({ page }) => {
    // Login
    await page.goto('/login');
    await page.fill('input[name="email"]', 'admin@example.com');
    await page.fill('input[name="password"]', 'password');
    await page.click('button[type="submit"]');
    await page.waitForURL('/dashboard');
  });

  test('should display API keys page', async ({ page }) => {
    await page.goto('/settings');
    await page.click('text=API Keys');
    await expect(page.locator('h2')).toContainText('API Keys');
  });

  test('should create new API key', async ({ page }) => {
    await page.goto('/settings');
    await page.click('text=API Keys');
    await page.click('button:has-text("Create New Key")');

    await page.fill('input#name', 'E2E Test Key');
    await page.check('input[value="resources:read"]');
    await page.click('button:has-text("Create Key")');

    await expect(page.locator('text=API Key Created')).toBeVisible();
    await expect(page.locator('code')).toContainText('sk_');
  });

  test('should revoke API key', async ({ page }) => {
    await page.goto('/settings');
    await page.click('text=API Keys');

    // Find first revoke button and click
    const rows = page.locator('tbody tr');
    const count = await rows.count();
    if (count > 0) {
      await rows.first().locator('button').click();
      // Confirm dialog if present
    }
  });
});
```

**Step 2: Create Audit Logs E2E test**

Create `frontend/e2e/audit-logs.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('Audit Logs', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/login');
    await page.fill('input[name="email"]', 'admin@example.com');
    await page.fill('input[name="password"]', 'password');
    await page.click('button[type="submit"]');
  });

  test('should display audit logs page', async ({ page }) => {
    await page.goto('/settings');
    await page.click('text=Audit Logs');
    await expect(page.locator('h2')).toContainText('Audit Logs');
  });

  test('should filter audit logs', async ({ page }) => {
    await page.goto('/settings');
    await page.click('text=Audit Logs');

    await page.fill('input[placeholder*="Filter by action"]', 'login');
    await page.waitForTimeout(500); // Debounce

    // Verify filtered results
    const rows = page.locator('tbody tr');
    const count = await rows.count();
    expect(count).toBeGreaterThan(0);
  });
});
```

**Step 3: Run E2E tests**

Run: `cd frontend && pnpm test:e2e`
Expected: All E2E tests pass

**Step 4: Commit**

```bash
git add frontend/e2e/
git commit -m "test(e2e): add API Keys and Audit Logs end-to-end tests"
```

---

## Task 14: Documentation

**Files:**
- Create: `docs/api-keys.md`
- Create: `docs/audit-logs.md`

**Step 1: Create API Keys documentation**

Create `docs/api-keys.md`:

```markdown
# API Keys

## Overview

API Keys allow users to authenticate requests to SkillHub APIs programmatically.

## Creating an API Key

1. Navigate to Settings → API Keys
2. Click "Create New Key"
3. Enter a name and select required scopes
4. Click "Create Key"
5. **Copy the key immediately** - it won't be shown again!

## Using an API Key

Include the key in the Authorization header:

\`\`\`bash
curl -H "Authorization: Bearer sk_your_key_here" \\
  https://api.skillhub.com/api/v1/resources
\`\`\`

## Scopes

- `resources:read` - Read resources
- `resources:write` - Create/update resources
- `skills:read` - Read skills
- `skills:call` - Call skills
- `skills:write` - Create/update skills
- `acl:read` - Read ACL rules
- `acl:write` - Modify ACL rules
- `tokens:read` - Read tokens
- `tokens:write` - Create/update tokens

## Security Best Practices

- Never commit API keys to version control
- Rotate keys regularly
- Use minimal scopes required
- Revoke unused keys
- Set expiration dates for temporary access
```

**Step 2: Create Audit Logs documentation**

Create `docs/audit-logs.md`:

```markdown
# Audit Logs

## Overview

Comprehensive audit trail of all system actions for security monitoring and compliance.

## Viewing Audit Logs

1. Navigate to Settings → Audit Logs (requires admin role)
2. Use filters to find specific events
3. Click on log entries for detailed information

## Filter Options

- **Search**: Free-text search
- **Action**: Filter by action type (e.g., `login`, `resource.create`)
- **Resource Type**: Filter by resource type
- **Date Range**: Filter by time period
- **Page Size**: Adjust results per page (20/50/100)

## Action Types

See design document for complete list of audit actions.

## Data Retention

Audit logs are retained for 90 days by default. Configure via `AUDIT_LOG_RETENTION_DAYS`.

## Export

Admin users can export audit logs to CSV for compliance reporting.
```

**Step 3: Commit**

```bash
git add docs/
git commit -m "docs: add API Keys and Audit Logs documentation"
```

---

## Phase 1.1a Complete Checklist

- [x] API Key database model and migration
- [x] API Key service layer with tests
- [x] API Key API endpoints
- [x] API Key authentication middleware
- [x] Audit Log database model and migration
- [x] Audit Log service and middleware
- [x] Audit Log API endpoints
- [x] Frontend Settings page structure
- [x] API Keys frontend page
- [x] Audit Logs frontend page
- [x] Remove tabs from ACL page
- [x] Environment configuration
- [x] E2E testing
- [x] Documentation

---

**Phase 1.1a Status:** ✅ Implementation Plan Complete

**Next Step:** Proceed to Phase 1.1b Implementation Plan
