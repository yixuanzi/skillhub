import { useEffect } from 'react';

export const ApiConfigDebug = () => {
  useEffect(() => {
    // Debug component - checks API configuration on mount
    // This component runs silently in the background
    // Check browser console for any configuration warnings
  }, []);

  return null;
};
