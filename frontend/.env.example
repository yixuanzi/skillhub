# API Configuration
#
# In development, use relative path to leverage Vite's proxy
# The proxy will forward requests to the backend server
VITE_API_BASE_URL=/api/v1

# For production deployment, uncomment and set the actual backend URL:
# VITE_API_BASE_URL=https://api.skillhub.example.com/api/v1
